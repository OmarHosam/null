#include "parser.h"
#include <stdio.h>

// typedef struct AST AST;
//
// struct AST {};

Token* parse(Token* tokens, int *length) {
  // TODO: Implement the parser.
  printf("%d\n", *length);
  return tokens;
}
